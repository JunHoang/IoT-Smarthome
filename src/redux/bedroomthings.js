import {BEDROOMTHINGS} from '../shared/bedroomthings';

export const Bedroomthings = (state = BEDROOMTHINGS, action) => {
    switch (action.type) {
        default:
            return state;
    }
};